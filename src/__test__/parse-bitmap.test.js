'use strict';

const parseBitmap = require('../lib/parse-bitmap');

describe('Testing should return magenta', () => {
  test('should return magenta'), () => {
    expect(process.argv[3]).toEqual('magenta');
  };
  test('Should return Yellow');
});

